import React from 'react';
import { Empty } from 'antd';

export default class Dashboard extends React.PureComponent {
  render() {
    return <Empty description="Hello, Re act & Mobx & TS & Babel" />;
  }
}
